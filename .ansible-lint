---
skip_list:
  - '106'
  - yaml # Ignore for now until we can auto-format it
  - git-latest # All this usage is in testing
  - package-latest
exclude_paths:
  - .cache/  # implicit unless exclude_paths is defined in config
  - .github/
  - docs/
parseable: true
use_default_rules: true
verbosity: 1